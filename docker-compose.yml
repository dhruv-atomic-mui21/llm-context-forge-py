version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=info
    volumes:
      - ./layoutlm_forge:/app/layoutlm_forge
    command: python -m uvicorn layoutlm_forge.api.app:app --host 0.0.0.0 --port 8000 --reload
